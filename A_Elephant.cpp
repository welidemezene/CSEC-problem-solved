#include <bits/stdc++.h> 
using namespace std;

int main() {
    int t;
    cin >> t;

    int count = (t + 4) / 5; // Calculate the minimum steps using division and round up
    cout << count << endl;

    return 0;
}
